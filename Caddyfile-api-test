{
   storage file_system /data
}

http://localhost:8080 {
   handle /api/login {
     reverse_proxy 127.0.0.1:4575
   }

   # Default to serving nothing or a 404 for anything else, to isolate the API
   respond 404
}
